# Documentation Changelog

Use this file to keep a lightweight trail of documentation-impacting changes. Keep entries short and focused on what changed, why it matters, and where follow-up docs live.

## Entry Format

| Date | Area | Change | Related Files |
| --- | --- | --- | --- |

## Entries

| 2026-08-20 | Portfolio rehaul: security, storage, rebrand, UI | Fixed signup privilege escalation (public signup always creates `employee`; client role stripped), added centralized route guards with server-side RBAC on candidates/resume/upload, same-origin CSRF checks on all cookie-auth mutations, failure-based login/signup rate limiting (Upstash-ready with in-process fallback), an audited admin-only user role-management API, SHA-256 duplicate fingerprints, duplicate-check-before-storage with compensating object deletion, private `r2://` resume URLs (no public base URLs on write), bounded ZIP decompression with ratio limits and dedicated tests, and file-signature validation. Rebranded from Amazon-derived identity to SkillMatch AI (indigo brand, new logo/favicon, neutral seed departments), added a signed-out landing page, gated demo credentials to demo mode, decomposed `app/dashboard.tsx` (~2970 → ~1590 lines) into `app/components/dashboard/*`, archived coursework artifacts to `docs/archive/coursework/`, rewrote the README for portfolio presentation with screenshots, added `docs/deployment.md` (Vercel + Neon + R2 + custom domain), and trimmed CI to install only the Chromium browser it runs with failure-artifact upload. | `lib/route-auth.ts`, `lib/rate-limit.ts`, `lib/validation.ts`, `lib/auth-model.ts`, `lib/db.ts`, `lib/storage.ts`, `lib/resume-parser.ts`, `lib/resume-upload-files.ts`, `app/api/**`, `app/components/**`, `app/dashboard.tsx`, `app/globals.css`, `app/layout.tsx`, `app/page.tsx`, `app/login/page.tsx`, `app/signup/page.tsx`, `tests/**`, `.github/workflows/ci.yml`, `README.md`, `docs/deployment.md` |
| 2026-05-06 | PA4 MVP demo updates | Clarified Workforce as an L&D skill-gap report grouped by department, employee group, and role family; expanded Settings with account/runtime/upload/access summaries and local demo preferences; improved PDF parser errors for blank, scanned/image-only, corrupt, and too-short PDFs; added candidate resume deletion with audit logging and storage cleanup where supported. | `app/dashboard.tsx`, `app/api/candidates/[id]/route.ts`, `app/api/settings/demo-preferences/route.ts`, `app/api/health/route.ts`, `lib/db.ts`, `lib/storage.ts`, `lib/resume-parser.ts`, `lib/learning-report.ts`, `lib/upload-config.ts`, `tests/resume-parser.test.ts`, `tests/candidate-delete.test.ts`, `tests/candidate-delete-route.test.ts`, `tests/dashboard-navigation.test.tsx`, `tests/e2e/admin-and-ld-views.spec.ts`, `tests/e2e/dashboard-role-navigation.spec.ts` |
| 2026-05-04 | PA4 MVP fixes | Added drag-and-drop upload E2E coverage, L&D-only skill-gap report grouped by department / employee group / role family, admin operational alerts (storage/database/upload + sync placeholder) with resolve workflow, and a tamper-evident audit hash chain plus admin audit filters. Audit and learning report routes are gated by role, and audit events now record actor email, role, and name. | `lib/audit-integrity.ts`, `lib/learning-report.ts`, `lib/db.ts`, `lib/storage.ts`, `app/dashboard.tsx`, `app/api/audit/route.ts`, `app/api/admin-alerts/route.ts`, `app/api/admin-alerts/[id]/resolve/route.ts`, `app/api/learning-report/route.ts`, `app/api/upload/route.ts`, `app/api/auth/login/route.ts`, `app/api/auth/logout/route.ts`, `app/api/auth/signup/route.ts`, `app/api/override/route.ts`, `app/api/candidates/[id]/learning-modules/route.ts`, `db/schema.sql`, `db/schema.ts`, `db/migrations/0006_admin_alerts_and_audit_chain.sql`, `tests/audit-integrity.test.ts`, `tests/admin-alerts.test.ts`, `tests/admin-alerts-route.test.ts`, `tests/learning-report.test.ts`, `tests/e2e/skillmatch.spec.ts`, `tests/e2e/admin-and-ld-views.spec.ts` |
| 2026-05-04 | Role-based navigation | Yash Baruah aligned dashboard navigation with role-based access rules for PA4 demo validation; restricted sections now disappear from nav while direct `?view=` access renders a restricted-access state. | `app/dashboard.tsx`, `app/page.tsx`, `lib/auth-permissions.ts`, `tests/dashboard-navigation.test.tsx`, `tests/e2e/auth-helpers.ts`, `tests/e2e/dashboard-role-navigation.spec.ts`, `tests/e2e/ui-visual.spec.ts` |
| 2026-05-04 | PA4 MVP validation | Yash Baruah added PA4 MVP validation coverage for target role selection. | `tests/e2e/skillmatch.spec.ts` |
| 2026-05-04 | PA4 repo cleanup | Yash Baruah clarified demo-mode signup behavior, added role-aware dashboard restrictions, and improved refresh loading/error states for candidate, history, saved-role, and audit data. | `app/login/page.tsx`, `app/signup/page.tsx`, `app/dashboard.tsx`, `app/globals.css`, `lib/auth-permissions.ts`, `lib/auth.ts`, `app/api/candidates/[id]/learning-modules/route.ts`, `tests/auth.test.ts`, `tests/candidate-learning-modules-route.test.ts`, `tests/e2e/skillmatch.spec.ts` |
| 2026-05-02 | UI / front-end | Global UI aligned with ui-ux-pro-max “Trust & Authority” / enterprise slate: slate neutrals and WCAG-friendly muted text, `--sidebar-bg` and inset amber active nav, 200 ms motion tokens, `prefers-reduced-motion`, and `focus-visible` rings (sky default, amber on primaries and header search via `:focus-within`). Inter applied on `<body>` with `next/font`. Also includes sidebar width, concept grid spacing, and prior dashboard/auth polish. | `app/globals.css`, `app/layout.tsx`, `app/dashboard.tsx` |
| 2026-05-02 | Docs process | Added repo-owned documentation workflow, changelog guidance, PR checklist prompts, and artifact organization rules for source vs generated docs. | `README.md`, `docs/README.md`, `docs/source/README.md`, `docs/generated/README.md`, `.github/pull_request_template.md` |
